# MCP Ecosystem Submissions

## Entry Text (for all lists)

```markdown
- [reprompt](https://github.com/reprompt-dev/reprompt) - Prompt analytics MCP server: score prompts, search history, detect leaked credentials, and scan AI coding sessions. Supports 9 AI tools.
```

## Submission Targets

### 1. punkpeye/awesome-mcp-servers (~84k stars) — HIGHEST PRIORITY

- Fork: https://github.com/punkpeye/awesome-mcp-servers
- Category: Development Tools (alphabetical under R)
- Rules: One server per PR, alphabetical, concise description
- Submit: PR

### 2. MCP Registry (Official Anthropic)

- Registry: https://github.com/modelcontextprotocol/registry
- Browse: https://registry.modelcontextprotocol.io/
- Namespace: `io.github.reprompt-dev/reprompt`
- Requires: Build publisher CLI, authenticate via GitHub OAuth
- Steps:
  1. `make publisher && ./bin/mcp-publisher`
  2. Authenticate with GitHub
  3. Create server.json
  4. Publish

### 3. appcypher/awesome-mcp-servers (~5.3k stars)

- Fork: https://github.com/appcypher/awesome-mcp-servers
- Category: Development Tools
- Rules: One item per PR, alphabetical, search for duplicates first

### 4. mcpservers.org

- Submit: https://mcpservers.org/submit
- This feeds wong2/awesome-mcp-servers (~3.8k stars)

## MCP Server Setup Instructions (for repo README)

```json
{
  "mcpServers": {
    "reprompt": {
      "type": "stdio",
      "command": "reprompt",
      "args": ["mcp-serve"]
    }
  }
}
```

## Tools Available (8 total)

1. `score_prompt` — Score any prompt 0-100 with research-backed analysis
2. `check_privacy` — Scan for leaked API keys, tokens, PII in prompt history
3. `search_prompts` — Search prompt history by keyword
4. `get_prompt_library` — Get reusable prompt patterns by category
5. `get_best_prompts` — Surface most effective prompts per category
6. `get_trends` — Track prompting skill evolution over time
7. `get_status` — Database statistics
8. `scan_sessions` — Scan new sessions incrementally
