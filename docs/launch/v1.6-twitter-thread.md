# v1.6 Twitter Thread

## Strategy

- NO external link in hook tweet (algorithmic suppression)
- Terminal screenshot/GIF essential (3x engagement vs text-only)
- Under 110 chars for hook tweet
- Link in final reply or bio
- Reply to every comment within 60 minutes

---

## Hook Tweet (110 chars)

I analyzed 1,000+ AI coding prompts. Found 3 leaked API keys and a 35% error loop rate in my agent sessions.

---

## Thread

**Tweet 2:**
Everyone analyzes what AI outputs. Nobody analyzes what humans type into AI tools.

I built reprompt -- a CLI that reads your existing Claude Code / Cursor / Codex session files and tells you what you're actually sending.

**Tweet 3:**
First surprise: `reprompt privacy --deep` found API keys and JWT tokens in my prompts.

Not pasted intentionally -- they were in error messages and stack traces I copy-pasted for debugging.

[screenshot of privacy --deep output]

**Tweet 4:**
Second surprise: 35% of my agent sessions have error loops.

The agent gets stuck retrying the same failing approach 3+ times. reprompt detects this by fingerprinting tool calls (Bash, Edit, Read) and their targets.

[screenshot of reprompt agent output]

**Tweet 5:**
Third: 50-70% of conversation turns are filler.

"ok try that", "continue", "looks good"

`reprompt distill` scores every turn using 6 signals and extracts only what matters. A 100-turn session -> 15-20 turns in <50ms.

**Tweet 6:**
All rule-based. No LLM, no network calls, no telemetry.

TF-IDF + regex + heuristics. Scoring calibrated against 4 NLP papers.

1,494 tests. MIT license. Works with 9 AI tools.

[screenshot of reprompt dashboard]

**Reply to thread (with link):**
Open source: github.com/reprompt-dev/reprompt

    pip install reprompt-cli
    reprompt scan && reprompt

Also works as an MCP server for Claude Code and a GitHub Action for CI quality gates.
